# План для Claude — старт M14 (новая сессия)

> **Назначение:** точка входа для следующего чата Claude Code.
> **Создано:** 2026-05-16
> **Контекст:** M14 plan approved. AG параллельно делает M14.1 (Меню).

---

## 1. Восстановление контекста (первые 5 минут сессии)

Прочитай в этом порядке:
1. `docs/M14_PLAN.md` — целиком (~600 строк). Это твоя спецификация.
2. `PROTOCOL.md` — последние 3-4 записи (2026-05-16+) для понимания статуса.
3. Этот файл — твой план.

**⚠️ Сюрприз на старте:** в момент создания этого плана работа AG над M14.1 была обнаружена в working tree, но не закоммичена. Ветка `m14-menu-categories-rating` создана. AG должен был доделать (юнит-тест + коммит) во вторник 2026-05-17. Проверь:

```bash
git branch --show-current     # ожидаемо: m14-menu-categories-rating
git log --oneline -5
```

- Если есть коммит `feat(menu): inline menu + examples (M14.1)` от AG → M14.1 закрыта, ревьюй его код (см. §3 ниже), потом параллельные задачи.
- Если коммита нет, но есть незакомиченные изменения в `src/bot/*` → AG не дозакрыл задачу. Зови Natalja, передавай ей `work/m14/ag-task-m14-1.md` для повторного запуска AG.

---

## 2. Что делать в первую сессию (параллельно с AG)

Две независимые задачи, не требующие AG-кода:

### Задача 1 (P0) — M14.4.1: 10 черновиков маршрутов

**Файл:** `work/m14/draft_routes.yaml`

**Входные данные:** 18 мест в KB (см. PROTOCOL `2026-04-25 (вечер)`):
```
domskij-sobor-riga, dom-chernogolovykh, tserkov-svyatogo-petra-riga,
rizhskij-zamok-riga, pamyatnik-svobody-riga, porohovaya-bashnya-riga,
shvedskie-vorota-riga, tri-brata-riga, koshachij-dom-riga,
rizhskij-rynok, nacionalnaya-opera, latvijskaya-natsionalnaya-biblioteka,
turajdskij-zamok, sigulda, peschera-gutmana, rundalskij-dvorets,
staraya-riga, riga
```

**10 маршрутов (имена ID — без пробелов, snake_case):**
1. `old_riga_1h` — Старая Рига экспресс
2. `old_riga_2h` — Старая Рига классика
3. `old_riga_4h` — Старая Рига + Центр + Рынок
4. `jugendstil_2h` — Югендстиль (Альберта, Элизабетес) ⚠ районы есть, но конкретных place_id для зданий нет — используй `riga` как обзорный + добавь TODO для Natalja
5. `quiet_center_2h` — Тихий центр + парки
6. `sigulda_day` — Сигулда + Турайда + Гутманя на день (~6ч)
7. `evening_riga_2h` — Вечерняя Рига с подсветкой
8. `rainy_day_3h` — На дождь (Домский собор, библиотека, рынок, кафе)
9. `with_kids_2h` — С детьми (Кошкин дом, Три брата, рынок)
10. `instagram_2h` — Самые «открыточные» точки (Три брата, Дом Черноголовых, Кошкин дом, Шведские ворота)

**Структура каждого маршрута** — точно по `M14_PLAN.md §7.3`:
```yaml
- route_id: old_riga_2h
  duration_min: 120
  name: "Старая Рига за 2 часа"
  vibe: "классика, must-see"
  start: "Памятник Свободы"
  waypoints:
    - place_id: pamyatnik-svobody-riga
      stay_min: 5
      transition: "идём на запад по бульвару"
    - place_id: domskij-sobor-riga
      stay_min: 20
      transition: "..."
  end_tip: "..."
  best_time: "утро или закат"
```

**Проверка:** каждый `place_id` должен быть из списка выше. Если предлагаешь place_id, которого нет — пометь `TODO_NATALJA: добавить место в KB` в комментарии.

**Acceptance:** файл `work/m14/draft_routes.yaml` с 10 шаблонами, готов к ревью Natalja. Время на задачу: ~1 сессия.

---

### Задача 2 (P1) — M14.6: 5 промптов под тон категорий

**Файлы:** новые в `src/rag/prompts/`

| Промпт | Тон | Образец/референс | Макс. слов |
|--------|-----|------------------|-----------|
| `food.j2` | «Друг советует» | `M14_PLAN.md §8.4` | 300 |
| `route.j2` | «Гид-проводник» | `M14_PLAN.md §7.5` | 400 |
| `transport.j2` | «Короткий, фактический» | новый, см. ниже | 150 |
| `lifehack.j2` | «Наставник» | новый, см. ниже | 200 |
| `short.j2` | Универсальная справка | новый, см. ниже | 100 |

**Что взять из existing:** структура входа (`{{ passages }}` + `{{ session_history }}`) и формат как в `src/rag/prompts/generator.j2`. Запреты на «заведение порадует», «гостеприимная атмосфера» — обязательны во всех 5.

**transport.j2 — каркас тона:**
```
Ты — справочная служба. Отвечай на конкретный вопрос: цена, время, как купить.
Никаких рассказов. Никакого «добро пожаловать в Ригу».
Если данных нет — честно скажи и предложи свободный текст.
Структура: 1) прямой ответ 2) лайфхак (если есть) 3) альтернативы (если есть).
```

**lifehack.j2 — каркас тона:**
```
Ты — местный, делишься опытом. Тон: «не делай X, лучше Y, потому что Z».
Без банальностей и листинга «10 советов». Только то, что реально работает.
Структура: 1) что НЕ делать 2) что делать 3) почему.
```

**short.j2 — каркас тона:**
```
1-2 факта в ответе. Без вступления, без эмодзи, без рассказов.
Идеально для «когда часы работы», «какой адрес», «сколько стоит».
```

**Acceptance:** 5 файлов созданы, тесты `tests/unit/test_prompts.py` проходят. Юнит-тесты можно расширить — фикстура → непустой текст после render.

---

## 3. После ревью M14.1 от AG (когда коммит появится)

1. **Прочитай коммит** AG целиком: `git show <hash>`.
2. **Проверки соответствия M14_PLAN.md §4:**
   - `MENU_TITLE`, `MENU_BUTTONS` (6 шт), `MENU_EXAMPLES` (3 шт) — в `i18n_ru.py`
   - `make_menu_keyboard()` в `ui.py` — 2×3 InlineKeyboard
   - Команда `/menu` отдельно + хвост `on_start`
   - 6 callback-роутов в `gateway.py` с заглушками
   - `menu:events` отвечает `EVENTS_COMING_SOON_TMPL`
3. **Проверки кода:**
   - Никаких изменений в `USER_SPEC.md`, `README.md`, существующих хендлерах
   - Branch — `m14-menu-categories-rating`, не master
4. **Если всё ок** — обновить PROTOCOL.md «M14.1 review passed». Сказать Natalja: «можно мержить + деплоить + тестировать UX».
5. **Если есть отклонения** — список замечаний в PROTOCOL + handoff обратно AG: «вернуть в M14.1, исправить пункты 1, 2, 3».

---

## 4. Дальше (M14.3 Рейтинг — основной CRITICAL PATH)

После того как M14.1 в мастере, следующая большая задача — M14.3 Рейтинг.

**Зона Claude в M14.3** (`M14_PLAN.md §6.2`):
- ~~14.3.4 — удалено~~
- 14.3.9 — генерация `story_id` (UUID4) и сохранение в `stories` после каждого `generate` в `gateway.py`. Тонкое место: не сохранять короткие справки (топик=transport/lifehack — без рейтинга места, только генерик).
- 14.3.12 — `format_place_card(place_id)` — карточка места из `M14_PLAN.md §6.7`

**Параллельно AG делает:** миграции SQLite (14.3.1), CRUD (14.3.2/3/5), клавиатуры (14.3.6/7), callback'и (14.3.8), rate-limit (14.3.11), `/top` (14.3.10), тесты (14.3.13).

**Распараллеливание Claude/AG возможно:** AG может начать с миграции SQLite, Claude параллельно пишет `story_id` + `format_place_card` + расширение генератора. Точка синхронизации — `RatingsStore` (AG) ↔ `gateway.py` (Claude).

---

## 5. Известные риски сессии

- **Лимит Claude может закончиться** — после каждой подзадачи коммитить отдельно. Маршруты `draft_routes.yaml` — отдельный коммит. Каждый промпт — желательно отдельным коммитом или объединить 5 в один «feat(prompts): M14.6 категорийные промпты».
- **AG ещё работает на M14.1** — не трогать `i18n_ru.py`, `ui.py`, `gateway.py` до его коммита.
- **Promt-файлы — зона Claude**, AG их не трогает.
- **Маршруты ссылаются на place_id вне KB** — пометить `TODO_NATALJA`, не выдумывать новые id.

---

## 6. Завершение сессии

В конце дня:
1. Обновить PROTOCOL.md записью «2026-05-XX | M14.X.Y Claude»
2. Если есть незавершённое — обновить memory `project_riga_guide_next_session.md`
3. Коммит всех изменений
4. Сказать Natalja: «сделал X, Y, Z. Следующее — A».

---

## 7. Связанные файлы

- `docs/M14_PLAN.md` — спецификация
- `work/m14/ag-task-m14-1.md` — что делает AG параллельно
- `PROTOCOL.md` — журнал
- `docs/USER_SPEC.md` — продуктовые ограничения (не нарушать)
- `docs/TECH_SPEC.md` — технические контракты
- `docs/ARCHITECTURE.md` — общая структура
