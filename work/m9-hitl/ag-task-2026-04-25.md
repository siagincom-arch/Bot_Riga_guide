# Riga Guide — задача для Antigravity (2026-04-25)

**Контекст:** Claude в новом чате после реcет лимита запускает HITL прогон на свежей KB (19 мест). Тебе работа параллельная — ничего не блокирует Claude, но к моменту анализа HITL пригодится.

## Что уже сделано тобой в этой сессии

- ✅ Миграция `google-generativeai` → `google-genai` в `src/llm/gemini.py`.
- ✅ `pyproject.toml`: новая зависимость + восстановлен `setuptools.build_meta`.
- ✅ `docs/ARCHITECTURE.md`: dependency map обновлена.
- ✅ `src/rag/prompts/tagger.j2`: переписан на транслит кириллицы.
- ✅ `docs/hitl_text_pack.yaml`: 18 запросов с `expected_tags`.
- ✅ `tests/ingest/test_tagger.py`: класс `TestTagChunkTransliteration`.
- ✅ `ingest/seeds/riga.yaml`: 19 мест.
- ✅ `tests/integration/test_rag_graph.py`: проверено что `FakeGeminiClient` совместим без изменений.

Всё это уже **закоммичено Claude** в master:
```
1e79f2d feat(llm+tagger): migrate to google-genai SDK, tagger → transliteration, HITL text_pack
```

---

## Задачи на эту сессию

### Задача 1 — DEPLOY.md обновить под новый SDK (P0)

**Файл:** `DEPLOY.md`.
**Что сделать:**
1. Найти все упоминания `google-generativeai` → заменить на `google-genai`.
2. В разделе требований (если есть) — указать `google-genai>=1.0`.
3. Если в разделе про API key есть инструкция «как настроить» — обновить под новый SDK (теперь ключ передаётся в `genai.Client(api_key=...)`, а не через `genai.configure()`).
4. Проверить что в smoke-тест чеклисте после деплоя есть пункт про прогон ingest на 1 месте.

Зачем: DEPLOY.md — единственный источник истины при выкатке на VPS. Сейчас он ссылается на старый SDK и собьёт первого читателя.

### Задача 2 — Smoke-фото для HITL (P1)

**Файлы:**
- `tests/fixtures/photos/README.md` — уже есть, описывает что класть.
- `tests/fixtures/photos/01_dome_cathedral.jpg` — нет.
- `tests/fixtures/photos/02_blackheads.jpg` — нет.
- `tests/fixtures/photos/03_freedom_monument.jpg` — нет.
- `tests/fixtures/photos/04_riga_castle.jpg` — нет.
- `tests/fixtures/photos/05_unknown.jpg` — нет.

**Что сделать:** скачать 5 фото из Wikimedia Commons (CC-BY/PD лицензии):

| Файл | Запрос Wikimedia | Ожидаемый `place_id` |
|------|------------------|----------------------|
| `01_dome_cathedral.jpg` | "Riga Cathedral" exterior | `domskij-sobor-riga` |
| `02_blackheads.jpg` | "House of the Blackheads Riga" | `dom-chernogolovykh` |
| `03_freedom_monument.jpg` | "Freedom Monument Riga" | `pamyatnik-svobody-riga` |
| `04_riga_castle.jpg` | "Riga Castle" | `rizhskij-zamok` |
| `05_unknown.jpg` | любой нелатвийский памятник (например, Kremlin Spasskaya Tower) | `not_recognized` ожидаем |

Размер каждого файла ≤ 500KB (Vision API не нужно 4K). Положить в `tests/fixtures/photos/`. **Source-атрибуцию** записать в `tests/fixtures/photos/README.md` (URL + автор + лицензия) — это важно для распространения проекта.

Зачем: HITL без фото = только текстовое покрытие. Полный M9.5 требует ≥3 фото-кейсов.

### Задача 3 (опциональная, если время есть) — Smoke-test для daily_rollup (P2)

**Файл:** `scripts/daily_rollup.py`.
**Что сделать:** проверить что скрипт работает на новом формате логов. Сгенерировать тестовый JSONL:
```bash
mkdir -p logs
cat > logs/bot_test.jsonl <<'EOF'
{"event": "request_log", "ts": "2026-04-25T10:00:00+00:00", "chat_id": 1, "input_type": "text", "landmark_id": "domskij-sobor-riga", "latency_ms": 4500, "status": "ok"}
{"event": "request_log", "ts": "2026-04-25T10:01:00+00:00", "chat_id": 1, "input_type": "photo", "landmark_id": "dom-chernogolovykh", "latency_ms": 8500, "status": "ok"}
{"event": "request_log", "ts": "2026-04-25T10:02:00+00:00", "chat_id": 2, "input_type": "text", "landmark_id": null, "latency_ms": 3000, "status": "not_recognized"}
EOF
python scripts/daily_rollup.py logs/bot_test.jsonl
```

Если скрипт падает или выдаёт некорректные метрики — поправить. Зачем: daily_rollup — это контракт на M2/M3/M5, его поломка незаметна до первого реального деплоя.

---

## Чего НЕ делать

- **Не трогать `src/`-код, кроме явно перечисленного.** Claude параллельно работает с RAG-графом и можно перетереть друг друга.
- **Не запускать `docker compose up bot`.** Это сломает HITL-прогон Claude (порты Telegram getUpdates).
- **Не править `data/`** — KB только что наполнилась, не сноси её.

---

## Handoff

После выполнения задач — обнови `PROTOCOL.md` своей записью «🛠️ Antigravity 2026-04-25», коммит на отдельных файлах. Если возникнут конфликты с master (Claude параллельно коммитит) — сделай `git pull --rebase`, не `merge`.

Дальнейшие задачи определит Natalja после анализа HITL CSV.
